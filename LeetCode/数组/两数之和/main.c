#include <stdio.h>

int* twoSum(int* nums, int numSize, int target, int* returnSize){
    // to do
    int a=5;
    return &a;
}

int main(){
    // define data
    int a = 0;
    int b = 1;
    // test
    printf("%d, %d\n", a+++b, a);
    return 0;
}